const ctrlWrapper = require("./ctrlWrapper");
const validateSchema = require("./validateSchema");
const auth = require("./auth");
const upload = require("./upload");

module.exports = { ctrlWrapper, validateSchema, auth, upload };
